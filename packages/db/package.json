{
  "name": "@fastifly/db",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "dev": "tsc -p tsconfig.json --watch",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsgo -p tsconfig.json --noEmit",
    "typecheck:tsc": "tsc -p tsconfig.json --noEmit",
    "lint": "biome check .",
    "test": "vitest run",
    "test:sqlite": "vitest run src/__tests__/sqlite-migrations.test.ts",
    "test:postgres": "vitest run src/__tests__/postgres-migrations.test.ts",
    "db:generate:sqlite": "node -e \"console.error('SQLite migration generation is not wired yet. Add drizzle-kit config before using this command.'); process.exit(1)\"",
    "db:generate:postgres": "node -e \"console.error('PostgreSQL migration generation is not wired yet. Add drizzle-kit config before using this command.'); process.exit(1)\"",
    "db:migrate:sqlite": "node -e \"console.error('SQLite production migration command is not wired yet. Use tested migration helpers only.'); process.exit(1)\"",
    "db:migrate:postgres": "node -e \"console.error('PostgreSQL production migration command is not wired yet. Use tested migration helpers only.'); process.exit(1)\""
  },
  "dependencies": {
    "@electric-sql/pglite": "^0.4.5",
    "@fastifly/common": "workspace:*",
    "better-sqlite3": "^12.9.0",
    "drizzle-orm": "^0.45.2"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^24.12.3",
    "typescript": "latest"
  }
}
